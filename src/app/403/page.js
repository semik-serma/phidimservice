import AccessDeniedCard from "@/components/access/AccessDeniedCard";

export const metadata = { title: "403 — Access Denied" };

export default function AccessDeniedPage() {
  return <AccessDeniedCard />;
}